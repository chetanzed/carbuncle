// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyCsPUvAxCQA5GoA-rRvVhK84x0QymBUANQ',
    authDomain: 'el-carbuncle.firebaseapp.com',
    databaseURL: 'https://el-carbuncle.firebaseio.com',
    projectId: 'el-carbuncle',
    storageBucket: 'el-carbuncle.appspot.com',
    messagingSenderId: '128171424483',
    appId: '1:128171424483:web:484acb28f2929966'
  }
}

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
